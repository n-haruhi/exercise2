<h3>Users</h3>

<% @users.each do |user| %>

  <p>image</p>
  <%= image_tag user.profile_image, size: "100×100" %>

  <p>name</p>
  <%= user.name %>

  <%= link_to "Show", user_path(user.id) %>

<% end %>